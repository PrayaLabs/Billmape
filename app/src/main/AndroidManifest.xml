<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dataentryapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:label="@string/app_name"
        android:theme="@style/Theme.DataEntryApp">
        <activity android:name=".Register" android:label="Register your Account"/>
        <activity android:name=".UserRecycler" android:label="Users Directory">
        </activity>
        <activity android:name=".Recyclerview" android:label="About"/>
        <activity android:name=".CreditEntry" android:label="Form"/>
        <activity android:name=".EntryRecycler" android:label="Entries"/>
        <activity android:name=".MainActivity" android:label="Data Entry App" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>